version: 2

sources:
  - name: raw
    database: "{{ env_var('SNOWFLAKE_DATABASE', 'MARKET_INTELLIGENCE_DB') }}"
    schema: RAW
    tables:
      - name: GITHUB_REPOSITORY_SNAPSHOT
        columns:
          - name: INGESTED_AT
            tests:
              - not_null
          - name: REPOSITORY_NAME
            tests:
              - not_null
